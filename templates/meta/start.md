# Start Here

This onboarding layer is installed for `{{projectName}}` with the `{{blueprint}}` blueprint.
Repo mode: `{{repoMode}}`. Stack: `{{stack}}`.

Installed system:

- agents: `{{selectedAgents}}`
- prompts: `{{availablePrompts}}`
- workflows: `{{availableWorkflows}}`

Use `orchestrator mode` for multi-step work, cross-file changes, or anything that may need subagents. Use `fast mode` for small, bounded tasks that should finish in one pass.

## Do This

1. If the task is small, open `{{fastModePromptPath}}`.
2. If the task is broad, open `{{orchestratorPromptPath}}`.
3. Keep one orchestrator responsible when work spans multiple files or roles.
4. Use `{{quickReferencePath}}` when you need the short map.
