# Before Notes

- repoPath: `/example/existing-node-api`
- repoMode: `brownfield`
- detectedStack: `node-api`
- detectedSignals: `package-json, api-dep, node-entry`
- suggestedBlueprint: `internal-tools`
- selectedBlueprint: `internal-tools`
- selectedInstallMode: `safe`

Notes:
- The repo already behaves like a service or admin backend.
- `internal-tools` prioritizes correctness, handoffs, and release safety.
- `safe` mode is appropriate for a first install into an existing API codebase.
