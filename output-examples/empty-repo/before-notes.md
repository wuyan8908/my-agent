# Before Notes

- repoPath: `/example/empty-repo`
- repoMode: `greenfield`
- detectedStack: `empty`
- detectedSignals: `none`
- suggestedBlueprint: `core`
- selectedBlueprint: `core`
- selectedInstallMode: `safe`

Notes:
- The repo has no existing product structure.
- `core` is the fallback blueprint because no domain signals exist.
- `safe` mode is enough because there are no generated files to reconcile yet.
